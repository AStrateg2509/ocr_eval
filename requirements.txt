# Базовые зависимости для запуска OCR-моделей в Colab / Kaggle
# Версии подобраны для совместимости с CUDA 12.1 (Colab T4/A100/L4)

# --- ядро ---
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.40.0
accelerate>=0.30.0
sentencepiece>=0.2.0
einops>=0.7.0
timm>=0.9.16
safetensors>=0.4.2
addict

# --- работа с PDF / изображениями ---
pdf2image>=1.17.0
pillow>=10.2.0
opencv-python-headless>=4.9.0.80
PyMuPDF>=1.23.26

# --- датасет / I-O ---
datasets>=2.18.0
huggingface-hub>=0.23.0
pyyaml>=6.0.1
tqdm>=4.66.2
pandas>=2.1.4
numpy>=1.26.4

# --- метрики OCR / Layout ---
nltk>=3.8.1
sacrebleu>=2.4.0
editdistance>=0.8.1
rapidfuzz>=3.6.2
apted>=1.0.3        # для TEDS (table edit distance similarity)
distance>=0.1.3
zss>=1.2.0          # zhang-shasha tree edit distance
shapely>=2.0.3      # IoU полигонов для layout

# --- визуализация / отчёты ---
matplotlib>=3.8.2
seaborn>=0.13.2
plotly>=5.20.0
jupyter>=1.0.0
ipywidgets>=8.1.2

# --- модели VLM (некоторые загружаются через trust_remote_code) ---
qwen-vl-utils>=0.0.8     # утилиты для qwen-vl-семейства, которое использует MonkeyOCR/DocOwl
flash-attn; platform_system != "Darwin"   # опционально, ускоряет инференс на A100/L4

# --- olmOCR-специфика ---
# olmOCR в Colab проще ставить через `pip install olmocr` после системных deps
# (poppler-utils, ttf-mscorefonts-installer) — см. notebooks/04_olmocr.ipynb
olmocr>=0.1.45 ; python_version >= "3.10"
